import 'package:flutter/material.dart';
import 'package:wac2_flutter/local_storage/sp_helper.dart';

class LocalStorageTest extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    // TODO: implement build
    return Scaffold(
      appBar: AppBar(
        title: Text('sphelper'),
      ),
    );
  }
}
